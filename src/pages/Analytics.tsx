import PageHeader from '@/components/layout/PageHeader';
import StubBody from '@/components/layout/StubBody';

export default function Analytics() {
  return (
    <>
      <PageHeader title="Analytics" subtitle="Opens, clicks, and signups for each campaign." />
      <StubBody note="Per-campaign analytics — delivery, opens, clicks, conversions — arrive in Phase E." />
    </>
  );
}
