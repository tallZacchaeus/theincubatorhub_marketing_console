import PageHeader from '@/components/layout/PageHeader';
import StubBody from '@/components/layout/StubBody';

export default function Settings() {
  return (
    <>
      <PageHeader title="Settings" subtitle="Sending setup, unsubscribes, and your account." />
      <StubBody note="Sending configuration, suppression list, and account settings arrive in Phase E." />
    </>
  );
}
